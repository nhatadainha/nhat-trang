let DB;
const $=id=>document.getElementById(id);
async function api(u,o={}){let r=await fetch(u,o);let d={};try{d=await r.json()}catch{}if(r.status===401)throw Error("unauthorized");if(!r.ok)throw Error(d.error||"Có lỗi xảy ra");return d}
function toast(t){let x=document.createElement("div");x.className="toast";x.textContent=t;document.body.appendChild(x);setTimeout(()=>x.remove(),2200)}
async function login(){try{await api("/api/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:$("u").value,password:$("p").value})});$("login").classList.add("hide");$("app").classList.remove("hide");show("dashboard")}catch(e){$("err").textContent=e.message}}
async function logout(){await api("/api/logout",{method:"POST"});location.reload()}
async function load(){DB=await api("/api/site")}
function setActive(page){document.querySelectorAll("nav button").forEach(b=>b.classList.toggle("active",b.dataset.page===page))}
function toggleSide(){document.querySelector("aside").classList.toggle("open")}
async function show(page){await load();setActive(page);if(page==="dashboard")dashboard();if(page==="settings")settings();if(page==="timeline")collection("timeline","Hành trình");if(page==="memories")collection("memories","Kỷ niệm");if(page==="gallery")gallery()}
function dashboard(){$("content").innerHTML=`<div class="topbar"><div><h1>Tổng quan ❤️</h1><p class="muted">Chào mừng bạn quay lại quản lý Love Story.</p></div><a class="btn" href="/" target="_blank">Xem website ↗</a></div><div class="stat-grid"><div class="stat"><b>${DB.timeline.length}</b><span>Mốc hành trình</span></div><div class="stat"><b>${DB.memories.length}</b><span>Kỷ niệm</span></div><div class="stat"><b>${DB.gallery.length}</b><span>Ảnh trong album</span></div></div><div class="panel"><h2>Website của bạn</h2><p class="muted">Mọi thay đổi từ trang Admin sẽ được lưu vào hệ thống hiện tại và hiển thị trên website chính.</p></div>`}
function settings(){$("content").innerHTML=`<div class="topbar"><div><h1>Thông tin chung</h1><p class="muted">Thiết lập nội dung nổi bật của trang chủ.</p></div></div><div class="panel"><h2>Nội dung</h2><div class="form"><input id="sn" value="${esc(DB.settings.coupleName)}" placeholder="Tên hai người"><input id="ss" value="${esc(DB.settings.subtitle)}" placeholder="Dòng giới thiệu"><input id="sd" type="date" value="${DB.settings.startDate||""}"><textarea id="sl" placeholder="Lời nhắn">${esc(DB.settings.letter||"")}</textarea><div class="toolbar"><button class="btn" onclick="saveSettings()">Lưu thay đổi</button></div></div></div><div class="panel"><h2>Ảnh bìa trang chủ</h2><div class="hero-preview"><img src="${DB.settings.heroImage||"/uploads/1787213750130-4syg4aqt387.jpg"}" onerror="this.style.display='none'"><div class="form"><input id="hero" type="file" accept="image/*"><small class="muted">Khuyên dùng ảnh ngang, chất lượng cao. Tối đa 10MB.</small><button class="btn secondary" onclick="uploadHero()">Đổi ảnh bìa</button></div></div></div>`}
async function saveSettings(){await api("/api/settings",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({coupleName:$("sn").value,subtitle:$("ss").value,startDate:$("sd").value,letter:$("sl").value})});toast("Đã lưu thay đổi ❤️");show("settings")}
async function uploadHero(){let f=$("hero").files[0];if(!f)return toast("Hãy chọn ảnh");let fd=new FormData();fd.append("photo",f);await api("/api/hero/upload",{method:"POST",body:fd});toast("Đã đổi ảnh bìa");show("settings")}
function esc(s){return String(s??"").replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[m]))}
function collection(k,label){$("content").innerHTML=`<div class="topbar"><div><h1>${label}</h1><p class="muted">Thêm, sửa hoặc xóa nội dung.</p></div></div><div class="panel"><h2>Thêm mới</h2><div class="form"><input id="fd" type="date"><input id="ft" placeholder="Tiêu đề"><textarea id="fc" placeholder="Nội dung"></textarea><button class="btn" onclick="add('${k}')">+ Thêm ${label.toLowerCase()}</button></div></div><div class="item-grid">${(DB[k]||[]).map(i=>`<div class="item"><small>${esc(i.date||"")}</small><h3>${esc(i.title||"")}</h3><p>${esc(i.description||"")}</p><div class="toolbar"><button class="btn secondary" onclick="edit('${k}',${i.id})">Sửa</button><button class="btn danger" onclick="del('${k}',${i.id})">Xóa</button></div></div>`).join("")||`<div class="empty">Chưa có dữ liệu.</div>`}</div>`}
async function add(k){if(!$("ft").value.trim())return toast("Nhập tiêu đề trước nhé");await api("/api/"+k,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({date:$("fd").value,title:$("ft").value,description:$("fc").value})});toast("Đã thêm ❤️");show(k)}
async function edit(k,id){let i=DB[k].find(x=>Number(x.id)===Number(id));let t=prompt("Tiêu đề",i.title||"");if(t===null)return;let d=prompt("Nội dung",i.description||"");if(d===null)return;let date=prompt("Ngày (YYYY-MM-DD)",i.date||"");if(date===null)return;await api("/api/"+k+"/"+id,{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:t,description:d,date})});toast("Đã cập nhật");show(k)}
async function del(k,id){if(!confirm("Bạn chắc chắn muốn xóa?"))return;await api("/api/"+k+"/"+id,{method:"DELETE"});toast("Đã xóa");show(k)}
function gallery(){$("content").innerHTML=`<div class="topbar"><div><h1>Album ảnh</h1><p class="muted">${DB.gallery.length} ảnh đang được lưu.</p></div></div><div class="panel"><h2>Thêm ảnh</h2><div class="form"><input id="photos" type="file" multiple accept="image/*"><input id="gt" placeholder="Tiêu đề chung (không bắt buộc)"><input id="gc" placeholder="Chú thích chung (không bắt buộc)"><button class="btn" onclick="upload()">Upload ảnh</button></div></div><div class="item-grid">${DB.gallery.map(i=>`<div class="item"><img src="${esc(i.src)}" loading="lazy"><h3>${esc(i.title||"Khoảnh khắc")}</h3><p>${esc(i.caption||"")}</p><button class="btn danger" onclick="del('gallery',${i.id})">Xóa ảnh</button></div>`).join("")||`<div class="empty">Album chưa có ảnh.</div>`}</div>`}
async function upload(){let fs=$("photos").files;if(!fs.length)return toast("Hãy chọn ảnh");let fd=new FormData();[...fs].forEach(f=>fd.append("photos",f));fd.append("title",$("gt").value);fd.append("caption",$("gc").value);await api("/api/gallery/upload",{method:"POST",body:fd});toast("Upload thành công ❤️");show("gallery")}
api("/api/me").then(x=>{if(x.loggedIn){$("login").classList.add("hide");$("app").classList.remove("hide");show("dashboard")}})
.catch(()=>{});
$("p").addEventListener("keydown",e=>{if(e.key==="Enter")login()});
