async function load(){let d=await fetch("/api/site").then(r=>r.json());name.textContent=d.settings.coupleName;sub.textContent=d.settings.subtitle;letter.textContent=d.settings.letter;days.textContent=Math.max(0,Math.floor((Date.now()-new Date(d.settings.startDate))/86400000)).toLocaleString("vi-VN");timeline.innerHTML=d.timeline.map(x=>`<article class="event"><time>${x.date}</time><h3>${x.title}</h3><p>${x.description||""}</p></article>`).join("");memories.innerHTML=d.memories.map(x=>`<article class="card"><h3>${x.title}</h3><small>${x.date||""}</small><p>${x.description||""}</p></article>`).join("");gallery.innerHTML=d.gallery.map(x=>`<img src="${x.src}" alt="${x.title||"Kỷ niệm"}">`).join("")}load();